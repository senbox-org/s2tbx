<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.esa.suhet</groupId>
        <artifactId>suhet</artifactId>
        <version>0.6.0</version>
    </parent>

    <packaging>jar</packaging>

    <artifactId>suhet-bootstrap</artifactId>
    <name>SUHET Bootstrap</name>
    <description>Provides the bootstrap classpath for all SUHET modules</description>

    <dependencies>

        <dependency>
            <groupId>org.esa.suhet</groupId>
            <artifactId>atm-corr-processor</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.esa.suhet</groupId>
            <artifactId>beam-sentinel2-reader</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.esa.suhet</groupId>
            <artifactId>beam-sentinel2-reader-new</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.esa.suhet</groupId>
            <artifactId>beam-sentinel3-reader</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

</project>
